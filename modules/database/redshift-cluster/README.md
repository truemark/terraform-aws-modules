# Redshift Cluster

Terraform submodule for `redshift-cluster`.

## Usage

```hcl
module "redshift_cluster" {
  source  = "truemark/truemark/aws//modules/database/redshift-cluster"
  version = "1.0.0"
}
```
